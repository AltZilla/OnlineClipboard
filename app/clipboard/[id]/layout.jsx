export const metadata = {
  title: 'Temporary Clipboard',
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
}

export default function ClipboardLayout({ children }) {
  return children
}
